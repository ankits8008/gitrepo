pipeline {
         agent any
         stages {
                 stage('One') {
                 steps {
                     echo 'Hi, this is Zulaikha from edureka'
                       }
                 }
          }
}
node('ubunutu') {
    stage("Checkout") {
        checkout scm
        sh 'git clean -fdx'
    }
}
