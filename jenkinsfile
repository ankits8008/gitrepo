pipeline {
         agent any
         stages {
                 stage('One') {
                 steps {
                     echo 'Hello, this is Zulaikha from edureka'
                       }
                 }
          }
}
node('master') {
    stage("Checkout") {
        checkout scm
        sh 'ls -l'
    }
}
